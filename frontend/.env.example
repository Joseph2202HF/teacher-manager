# URL de base de l'API PHP
# En développement, le proxy Vite redirige /api → http://localhost:8000
# En production, pointer directement vers le serveur PHP :
# VITE_API_URL=https://mondomaine.com/api
VITE_API_URL=/api
